spring:
  datasource:
    url: jdbc:h2:mem:crowfoot;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;INIT=CREATE SCHEMA IF NOT EXISTS crowfoot_core
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop        # 엔티티에서 스키마 생성 (스키마는 INIT으로 선 생성)
