package net.java21.crowfoot.api.account.domain;

/**
 * Workspace 세트 생성 공통 상수 — 가입 프로비저닝(개인 기본 WS)이 사용한다.
 */
public final class WorkspaceConstants {

    /** 기본 Workspace 이름 — 가입 시 자동 생성되는 기본 공간 */
    public static final String DEFAULT_WORKSPACE_NAME = "기본 워크스페이스";

    private WorkspaceConstants() {
    }
}
